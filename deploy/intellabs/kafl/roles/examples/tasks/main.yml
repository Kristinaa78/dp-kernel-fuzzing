- name: Clone repo "{{ examples_url }}"
  ansible.builtin.git:
    repo: "{{ examples_url }}"
    dest: "{{ examples_root }}"
    version: "kAFL-targets"
    depth: "{{ git_clone_depth | default(omit) }}"
    force: "{{ force_clone }}"
    key_file: "~/.ssh/ansible"
    recursive: false
  tags:
    - clone

- name: Remove .git directory from "{{ examples_root }}"
  ansible.builtin.file:
    path: "{{ examples_root }}/.git"
    state: absent
  tags:
    - clone

- name: Remove .gitignore file from "{{ examples_root }}"
  ansible.builtin.file:
    path: "{{ examples_root }}/.gitignore"
    state: absent
  tags:
    - clone

- name: Remove .gitmodules file from "{{ examples_root }}"
  ansible.builtin.file:
    path: "{{ examples_root }}/.gitmodules"
    state: absent
  tags:
    - clone

- name: Import template windows tasks
  ansible.builtin.import_tasks: template_windows.yml
  when: "'examples-template-windows' in ansible_run_tags"

- name: Import dvkm tasks
  ansible.builtin.import_tasks: dvkm.yml
  when: "'examples-linux-dvkm' in ansible_run_tags"
